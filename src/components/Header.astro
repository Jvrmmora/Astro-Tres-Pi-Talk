---

---

<div class="header-content">
  <div class="gradient-band"></div>
  <h1 class="main-title">3pi Talks!</h1>
  <p class="sub-title">
    AWS vs GCP vs Azure: entendiendo la nube más allá del logo
  </p>
</div>

<style>
  .header-content {
    text-align: center;
    position: relative;
    perspective: 1200px;
    transform-style: preserve-3d;
  }

  .main-title {
    font-size: clamp(3rem, 10vw, 11rem);
    font-weight: 800;
    margin-bottom: 0.2rem;
    color: #ffffff;
    letter-spacing: -0.035em;
    line-height: 1.02;
    transform: translateZ(-220px);
    text-shadow: 0 2px 0 rgba(255, 255, 255, 0.04),
      0 18px 60px rgba(0, 0, 0, 0.6);
    opacity: 0.96;
  }

  .sub-title {
    font-size: clamp(1rem, 2.6vw, 2rem);
    font-weight: 600;
    color: rgba(255, 255, 255, 0.9);
    letter-spacing: 0.01em;
    line-height: 1.35;
    margin-top: 0.25rem;
    margin-bottom: 0.75rem;
    transform: translateZ(-200px);
    text-shadow: 0 6px 28px rgba(0, 0, 0, 0.5);
  }

  .gradient-band {
    position: absolute;
    top: 40%;
    left: 50%;
    width: min(1200px, 90vw);
    height: 360px;
    transform: translate(-50%, -50%);
    background: radial-gradient(
        60% 60% at 50% 50%,
        rgba(255, 214, 165, 0.55),
        transparent 60%
      ),
      radial-gradient(
        60% 60% at 30% 60%,
        rgba(255, 137, 185, 0.45),
        transparent 60%
      ),
      radial-gradient(
        60% 60% at 70% 40%,
        rgba(160, 196, 255, 0.5),
        transparent 60%
      ),
      radial-gradient(
        60% 60% at 50% 80%,
        rgba(155, 246, 255, 0.45),
        transparent 60%
      );
    filter: blur(50px) saturate(110%);
    border-radius: 40px;
    z-index: -1;
    pointer-events: none;
  }

  @media (max-width: 768px) {
    .main-title {
      font-size: clamp(2.2rem, 9vw, 4rem);
      transform: translateZ(-140px);
    }
  }

  @media (max-width: 480px) {
    .main-title {
      font-size: clamp(1.9rem, 10vw, 3rem);
      transform: translateZ(-100px);
    }
  }
</style>